interface:
  display_name: "Git Master"
  short_description: "Prepare Git changes, PRs and CI safely"
  default_prompt: "Use $git-master to prepare and publish this change safely."
policy:
  allow_implicit_invocation: true
