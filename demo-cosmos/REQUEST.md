Fix `merge_settings(defaults, overrides)` for JSON configurations represented by dictionaries: merge dictionaries recursively, preserve keys that are not overridden, honor `False`, `0`, the empty string, and `None` as explicit values, replace whole lists, and do not share mutable structures with either input. Do not add dependencies. Demonstrate exploration, implementation, and independent review with the Cosmos skill; this three-stage flow is requested for validation even in this small example. Edit only `settings.py` and `test_settings.py`. Use `unittest`.
